{
  "name": "eureka-crm",
  "version": "2.0.0",
  "description": "Eureka Energy Consulting CRM - React/HTML",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js",
    "deploy": "cmd /c scripts\\deploy-local.bat",
    "start": "npx http-server ./dist -p 8080 -c-1",
    "serve": "npx http-server ./dist -p 8000 -c-1",
    "lint": "echo Tests pending",
    "test": "echo Tests pending",
    "clean": "rmdir /s /q dist node_modules"
  },
  "keywords": ["crm", "energy", "consulting", "eureka"],
  "author": "Eureka Energy Consulting",
  "license": "PROPRIETARY",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {}
}
